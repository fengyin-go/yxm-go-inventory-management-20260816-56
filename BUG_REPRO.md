# BUG_REPRO

## Bug 是什么
商品模块的关键词搜索、SKU 唯一性、字段与状态校验，以及库存建立时的引用校验失效。

## 如何触发
1. 创建两个不同 SKU 的商品。
2. 用不同大小写关键词搜索，或把商品更新为重复 SKU / 空名称 / 非法状态。
3. 给不存在的商品或仓库建立库存。
4. 运行 `go test ./...`。

## 错误信息
测试失败，搜索不到预期商品，重复 SKU、空名称、非法状态和不存在商品的库存均未按预期被拒绝。
